# 代码部署
## 1. 下载代码
主要是./backend和./frontend文件夹下的代码，这两个文件夹分别是后端和前端页面的代码。
## 2. 检查环境配置变量
- 文件检查一：./backend/.env.prod——生产环境配置文件，这个文件包含了数据库、redis等配置信息。 
具体示例：
```commandline
DJANGO_SETTINGS_MODULE=mycrm.settings.prod
DEBUG=False
SECRET_KEY=real-strong-production-secret
DATABASE_NAME=crm
DATABASE_USER=crm_user
DATABASE_PASSWORD=secure-prod-password
DATABASE_HOST=db
DATABASE_PORT=3306
ALLOWED_HOSTS=www.hcblj.top,api.hcblj.top
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://www.hcblj.top
REDIS_HOST=redis
```
- 文件检查二：./backend/.initial_data.json——初始数据文件，这个文件包含了MMCRM初始管理员信息以及示范数据。./backend/entrypoint.sh——初始数据加载脚本。 
这个文件是初始数据加载的脚本。
